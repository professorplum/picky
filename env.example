# Picky Meal Planner - Environment Configuration
# Copy this file to .env and customize for your local development

APP_NAME="picky"

# Environment name (dev, stage, prod)
ENV=dev

# Debug mode (true/false)
DEBUG=true

# Server settings
HOST=0.0.0.0
# PORT is handled by run.py and Azure, but you can set it here too

# CORS origins (comma-separated for multiple origins)
# For development, this is typically permissive
CORS_ORIGINS=*
# Example for production:
# CORS_ORIGINS=https://your-domain.com,https://www.your-domain.com

# Key Vault Configuration
KEY_VAULT_URL="https://picky-shared-kv.vault.azure.net/"

# Cosmos DB Configuration
COSMOS_ENDPOINT=https://your-cosmos-account.documents.azure.com:443/
# Dev database can be stage db in azure or local emulator, handled in secrets_service.py
# Database name is derived from Environment as picky-{env}-db
# Cosmos DB Emulator (commented out - use for local development)
# COSMOS_CONNECTION_STRING="AccountEndpoint=https://localhost:8081/;AccountKey=C2y6yDjf5/R+ob0N8A7Cgv30VRDJIWEHLM+4QDU5DE2nQ9nDuVTqobD4b8mGGyPMbIZnqyMsEcaGQy67XIw/Jw==;"
